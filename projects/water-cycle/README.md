# 水循环

模板 edu × 风格 vector-flat × 布局 flow-steps
流程详见 CONTENT.md 与 skill「pic-flow」（~/.dsh/skills/pic-flow/SKILL.md）。

> 本目录只保留**源文件**（分镜 / 素材清单 / 排版 / 风格包）。生成素材、渲染块、
> 成品长图均为可再生产物，不入仓库；成品示例见 `examples/`。
> 复现：配置好生图 Provider（见 skill README）后 `python3 scripts/gen_all.py`，
> 再按 SKILL.md 流程排版校准、拼接。
